# -----------------------------------------------------------------------------
# Environment variables for the Image Annotation System v2
#
# Copy this file to .env and fill in the values for local development.
# For AWS deployment, these variables are set in the CloudFormation templates
# or the Lambda function configurations.
# -----------------------------------------------------------------------------

# --- Flask Web Application Settings ---
# A secret key for Flask to use for signing sessions and other security purposes.
# Generate a random string for this.
FLASK_SECRET_KEY="a_super_secret_key_for_development"

# --- AWS RDS Database Connection ---
# The hostname or endpoint of the RDS MySQL instance.
DB_HOST="your-rds-instance-endpoint.region.rds.amazonaws.com"
# The database username.
DB_USER="your_db_user"
# The database password.
DB_PASSWORD="your_db_password"
# The name of the database to connect to.
DB_NAME="image_annotation_db"
# The database port (MySQL default is 3306).
DB_PORT="3306"

# --- AWS S3 Bucket Names ---
# The S3 bucket where original uploaded images are stored.
S3_IMAGE_BUCKET="your-unique-name-for-original-images-bucket"
# The S3 bucket where generated thumbnails are stored.
S3_THUMBNAIL_BUCKET="your-unique-name-for-thumbnails-bucket"

# --- Google Gemini API Settings (for Annotation Lambda) ---
# Your API key for the Google Gemini service.
GEMINI_API_KEY="your_google_gemini_api_key"
# The specific Gemini model to use for image captioning.
GEMINI_MODEL_NAME="gemini-1.5-flash-latest"
# The prompt to send to the Gemini API along with the image.
GEMINI_PROMPT="Describe this image in a single, descriptive sentence."

# --- Thumbnail Generation Settings (for Thumbnail Lambda) ---
# The target width for generated thumbnails in pixels.
THUMBNAIL_WIDTH="128"
# The target height for generated thumbnails in pixels.
THUMBNAIL_HEIGHT="128"
# The S3 key prefix for storing thumbnails in the thumbnail bucket.
THUMBNAIL_KEY_PREFIX="thumbnails/"

# --- General Application Settings ---
# The logging level for the application (e.g., DEBUG, INFO, WARNING, ERROR).
LOG_LEVEL="INFO"